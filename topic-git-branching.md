
# Git Branching

Git branching дозволяє створювати незалежні гілки для нових функцій або виправлень.

## Основні команди:
- `git branch` — показує всі гілки.
- `git checkout -b feature-name` — створює нову гілку та перемикає на неї.
- `git merge branch-name` — зливає гілку з поточною.
